node_modules/
.next/
dist/
build/
.expo/
*.log
.env
.env.local
.env.*.local

# ─── SECRET FILES — NEVER COMMIT ───────────────────────────────────────────
# Firebase service-account private keys (rotate immediately if already committed)
*-adminsdk-*.json
digitalsolution-76a79-firebase-adminsdk-fbsvc-772fd6fd3f.json

# Firebase Android client config (contains API key — restrict by package name)
# Technically safe in APK but keep out of git as defence-in-depth
google-services.json
GoogleService-Info.plist

# Twilio 2FA recovery codes
ref/twilio_2FA_recovery_code.txt

# Any private keys or credentials
*.pem
*.p8
*.p12
*.key
*service-account*.json
*credentials*.json

# Firebase Admin SDK credentials
*firebase-adminsdk*.json
digitalsolution-76a79-firebase-adminsdk-fbsvc-772fd6fd3f.json
