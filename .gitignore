node_modules/
.next/
dist/
build/
.expo/
coverage/
*.log
.env
.env.local
.env.*.local
*.tsbuildinfo
diagnostics*.json
lint-baseline.txt

# ─── SECRET FILES — NEVER COMMIT ───────────────────────────────────────────
# Firebase service-account private keys (rotate immediately if already committed)
*-adminsdk-*.json
digitalsolution-76a79-firebase-adminsdk-fbsvc-772fd6fd3f.json

# Firebase Android client config — scoped to root only.
# The RepairShopApp/google-services.json is tracked intentionally for EAS Build.
/google-services.json
/GoogleService-Info.plist

# Twilio 2FA recovery codes
ref/twilio_2FA_recovery_code.txt

# Any private keys or credentials
*.pem
*.p8
*.p12
*.key
*service-account*.json
*credentials*.json

# Firebase Admin SDK credentials
*firebase-adminsdk*.json
digitalsolution-76a79-firebase-adminsdk-fbsvc-772fd6fd3f.json
/google-services.json
