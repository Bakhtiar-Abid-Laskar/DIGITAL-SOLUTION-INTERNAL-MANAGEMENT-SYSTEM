Entity
public.export_jobs_latest
Issue
View public.export_jobs_latest is defined with the SECURITY DEFINER property

Description
Detects views defined with the SECURITY DEFINER property. These views enforce Postgres permissions and row level security policies (RLS) of the view creator, rather than that of the querying user

Entity
public.stuck_uploads
Issue
View public.stuck_uploads is defined with the SECURITY DEFINER property

Description
Detects views defined with the SECURITY DEFINER property. These views enforce Postgres permissions and row level security policies (RLS) of the view creator, rather than that of the querying user

