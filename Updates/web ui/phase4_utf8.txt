## 8. PAGE-BY-PAGE REBUILD SPEC\n+\n+For each page: route, file, what changes visually, and which audit issues get fixed as a byproduct of the rebuild (don't fix issues outside this list on a given page without flagging it â€” stay scoped per phase).\n+\n+### 8.1 Login â€” `/login` â€” `src/app/login/page.tsx`\n+\n+Reference: `ui_update.md` #1 (Role Selection), adapted â€” **assumption:** the admin panel keeps a **single login form**, not a 3-way role switcher, since role selection is how users enter the *mobile* app; admins reach this web panel directly. If this assumption is wrong, stop and confirm before building.\n+\n+- Re-theme the centered white card to the new tokens; canvas is now pure white behind it, so introduce a subtle full-bleed background treatment (e.g. a very light purple radial gradient wash, or a thin decorative top brand bar) so the card doesn't disappear into an all-white page â€” a plain white card on a plain white page has zero contrast under the new palette, which the old cream canvas didn't have to worry about.\n+- Add the sidebar's `Wrench` icon mark above/beside the \"RepairShop\" title for brand continuity (fixes audit L13).\n+- Add a show/hide password toggle (`Eye`/`EyeOff` icon inside the password field) â€” fixes L12.\n+- Add `autoFocus` to the email input â€” fixes M14.\n+- Add a \"Forgot password?\" link below the form, right-aligned, small text, purple accent color, routing to whatever recovery flow exists or a \"contact your administrator\" state if none does yet â€” fixes H7.\n+- Map raw Supabase error strings through a small friendly-message lookup (timeout â†’ \"Connection issue, please try again\", rate-limit â†’ \"Too many attempts, please wait a moment\") before displaying in the error banner â€” fixes Issue 3 (LOW).\n+- Fix the submit button height to match the new unified button-height decision from Section 7.\n+\n+### 8.2 Overview / Dashboard â€” `/` â€” `src/app/(admin)/page.tsx`\n+\n+Reference: `ui_update.md` #12 (Admin Dashboard Web).\n+\n+- **Greeting banner:** new full-width gradient card (\"Welcome, Admin\" + short system-overview line + a small decorative bar-chart glyph on the right), using the purple accent as the gradient base (`admin-accent` â†’ `admin-accent-dark`) rather than the reference's blue, to stay consistent with this app's purple-as-brand decision from Section 4.4. Keep the existing Refresh action, moved into this banner or kept in the `PageHeader` â€” pick one, don't duplicate it in both places.\n+- **KPI cards:** keep the existing 4-card grid structure and hover-lift, re-themed. Rename/remap to match the new reference's more operationally relevant metrics if the underlying queries already support it (Total Jobs, Technicians, Customers, Revenue this month) â€” otherwise keep the current 4 (Jobs Today, Completed This Week, Active Technicians, Pending Approvals) since changing what's queried is a data-layer decision outside this prompt's scope. Flag which option you took.\n+- **Replace the two placeholder panels** (the bar chart with the wrong-palette tooltip, and the static \"Quick Stats\" card that says nothing â€” audit issues H6 and L1) **with:**\n+  - An **Alerts module** (left): a short prioritized list with color-coded severity icons â€” green for low-stock-style informational items, orange for pending-approval-style items, red for urgent/blocking items â€” reusing whatever data already powers \"Pending Approvals\" and inventory low-stock checks, just surfaced as a feed instead of a lone counter.\n+  - A **donut chart** (right) titled \"Today's Jobs Overview\" showing the same received/in-progress/completed/waiting breakdown the Jobs page already tracks, center label = total count. Use Recharts' `PieChart` with `innerRadius` for the donut effect, colored with the semantic status tokens (Section 4.5) â€” and make sure the tooltip/hover-cursor colors use the new neutral-grey tokens, not the old cool blue-grey defaults (fixes audit L1 directly, since this replaces the offending chart).\n+- **Recent Jobs table:** keep it below the fold, but make rows genuinely clickable (`cursor-pointer`, `onClick` â†’ job detail), matching the Jobs List page's already-correct pattern â€” fixes audit **H5**.\n+- Fix the \"Pending Approvals\" query distinction between newly-registered-pending users and intentionally-blocked users if the KPI is kept (audit M5) â€” this is a one-line query filter change (e.g. an explicit `pending` status/flag instead of overloading `is_active = false`), flagged here since it's a very small, low-risk data-layer touch directly tied to a UI card this phase is already rebuilding; confirm before changing the query if any uncertainty exists about the underlying schema.\n+- Add real dates to the chart's period label (e.g. \"Jul 11 â€“ Jul 17\" instead of just \"Last 7 Days\") â€” fixes L5/Issue 5.\n+\n+### 8.3 Jobs List â€” `/jobs` â€” `src/app/(admin)/jobs/page.tsx`\n+\n+Reference: `ui_update.md` #6 (Job Tracking).\n+\n+- **Tabs replace/augment the status filter:** add the reference's horizontal status tabs with live counts â€” All (25), Received (6), In Progress (9), Completed (7), Waiting (3) â€” using the new shared `Tabs` component (Section 7), with the underline using the purple accent. These can sit alongside or replace the existing Status `Select` â€” recommendation: keep tabs as the primary quick-filter (matches the reference exactly) and keep the Technician/Priority selects in the filter bar below for finer filtering, removing only the redundant Status select since the tabs now cover that job.\n+- **Search icons in the header, not just an inline field:** the reference shows dedicated search/filter icon buttons top-right; the current filter-bar-with-inputs pattern is more powerful (has separate Technician/Priority selects) so **keep the existing filter bar**, but move Search to trigger from a header icon that expands the search input, matching the reference's visual language without losing functionality. If this feels like it's fighting the reference too much, the simpler option is acceptable: keep the current always-visible search input styled to the new tokens. Pick whichever reads cleaner once built; note the choice in your phase summary.\n+- **Make search server-side** (fixes audit **M7**, MEDIUM): replace the client-side `.filter()` over the current page's 20 rows with a debounced Supabase `.ilike()` query against code/name/phone so search actually finds jobs outside the currently-loaded page.\n+- **Remove the redundant Eye icon** in the Actions column (audit L2/Issue 2) since the entire row is already clickable â€” this declutters the row.\n+- **Add a date-range filter** (created-date) to the filter bar â€” fixes audit M4 (Jobs page numbering â€” \"No Date Range Filter\" MEDIUM).\n+- Re-theme all status/priority pill colors to Section 4.5 tokens (they already map closely, verify hue consistency).\n+- **Reassign Technician modal:** bring its backdrop in line with the new standardized modal backdrop (Section 7) â€” fixes the specific inconsistency called out in audit Issue 3.\n+\n+### 8.4 Job Detail â€” `/jobs/[id]` â€” `src/app/(admin)/jobs/[id]/page.tsx`\n+\n+Reference: `ui_update.md` #15 (Bill Generation) for the Billing tab; other tabs have no direct reference, so keep current structure, re-themed, with the fixes below.\n+\n+- Convert the tab bar to the new shared `Tabs` component.\n+- **Billing tab redesign per the reference:** a two-column layout â€” left: itemized table (Item/Description, Qty, Amount) auto-populated from Materials plus a Labour Charges line; right or below: Sub Total â†’ Tax (18%) â†’ Discount â†’ **Total**, plus `Print`, `Email`, and `WhatsApp` action buttons matching the reference's icon-button row exactly. This is close to the current tab's structure already â€” mainly a visual/layout tightening plus adding the explicit itemized-line-item table instead of just raw inputs.\n+- **Kill every `alert()` call in this file** (audit C1, C4 â€” CRITICAL): Notes save, Billing save, invoice email send/fail, validation failures â€” all route through the new toast system instead.\n+- Replace the raw Notes `<textarea>` with the new `Textarea` component (audit L5/Issue 5).\n+- Change the Materials tab icon from `Plus` to `Package` or `Layers` for semantic accuracy (audit L2/Issue 2, LOW).\n+- Add visible labels above the 3 inline Add-Material inputs instead of relying on placeholder-only text (audit L3/Issue 3).\n+- Make the customer contact number in the Overview tab a `tel:` link (and/or WhatsApp deep link) instead of static text (audit L6/Issue 6).\n+- Breadcrumb (\"Jobs / RS-2026-XXXX\") now comes from the Topbar rebuild (Section 6.2) â€” don't duplicate it in the page header too.\n+\n+### 8.5
